# Instructions

## Testing

Use agent-browser to test your changes.

## Design Guidelines

- Don't use card heavy design.
- No rounded corners.
- Do not use gradients anywhere.

## Lint & Format

Run `npm run typecheck` (typescript), `npm run lint` (oxlint) and `npm run format` (oxfmt) after making changes.
